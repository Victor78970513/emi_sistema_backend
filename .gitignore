# Directorios de Node.js / Dependencias
node_modules/
dist/
build/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-store/
.pnpm-state.json

# Archivos de configuración y entornos (¡Muy importante!)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.DS_Store # Archivos específicos de macOS
.vscode/ # Configuración de VS Code específica del usuario (opcional, si no quieres compartirla)

# Archivos de logs
logs/
*.log
npm-debug.log*
yarn-debug.log*

# Compilación de TypeScript
*.tsbuildinfo

# Build artefacts (Frontend React/Vite)
# Asegúrate de que tu carpeta de build del frontend (dist o build)
# esté incluida aquí si está dentro del mismo repositorio.
# Por ejemplo, si tu frontend está en ./frontend/dist
# frontend/dist/
# frontend/build/

# Archivos de sistema operativo / temporales
.DS_Store
Thumbs.db
ehthumbs.db
ehthumbs_vista.db
*.stackdump
npm-debug.log
yarn-debug.log
yarn-error.log

# Editor de texto/IDE específicos
.idea/ # Directorios de WebStorm/IntelliJ
.vscode/ # Directorios de VS Code (a veces se ignoran, a veces se incluyen para config compartida)
*.sublime-project
*.sublime-workspace

# Archivos de pruebas y cobertura
coverage/
.nyc_output/
*.lcov